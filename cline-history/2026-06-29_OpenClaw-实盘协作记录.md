# 2026-06-29 OpenClaw 实盘协作记录

> 实盘引擎首次完整运行 + 问题修复全过程

## 时间线

### 阶段一：启动验证（11:56 - 12:02）

Cline 完成重构（废弃单体 LiveEngine，回归 DP/SM/OM 三线程架构）后，OpenClaw 首次启动 `run_live.py`。

```
✅ 首次策略 tick @ 12:01:46
✅ 监控每30s稳定运行
✅ 0 错误
```

### 阶段二：OM 线程崩溃（12:04 - 14:08）

**问题**: 运行 ~2h 后状态文件卡住 2.5h 不更新

```
ERROR:src.engine.order_manager:[OM] Error: 'NoneType' object has no attribute 'loc'
```

**根因**:
- `rawdata_show` 只在回测模式初始化，实盘为 `None`
- OM 线程 `except: break` 直接退出
- 主循环 `dp_queue.join()` 死锁

**处理**: 报告用户 → Cline 修 → 重启 ✅

### 阶段三：Cline 修复 + LiveLogger 上线（17:21 - 22:16）

Cline 提交了两个修复 commit：
1. `8369985 fix: 实盘信号竞态条件 + 指标空值 + 日志缺失`
2. `47ce68f feat: 实盘持久化日志模块 LiveLogger`

修复内容：
- `rawdata_show` None guard
- `indicators: {}` → 传真实指标值
- Event 同步机制替代 queue.join 死锁
- `src/data/live_logger.py` 新模块 → CSV 日志到 `reports/live_*/`
- SM thread 增加 `processing_done` Event

**验证**：确认指标正确写入状态文件和 CSV ✅

### 阶段四：稳定运行（22:16 - 23:11+）

```
📊 监控: 115次 tick，0风险事件
📈 策略: 11次 tick，signal=0
🚫 错误: 0
📁 CSV: strategy_ticks.csv + monitor_ticks.csv 完整记录
```

MA10 走平在 1568 附近，MA30 从 1577 持续下行至 1572，差价收窄至 -3.75。

## 关键配置

### config.ini
```ini
[LiveEngine]
strategy_interval = 300    # 5分钟
monitor_interval = 30      # 30秒
risk_max_daily_loss_pct = 5
risk_min_balance = 10.0

[AlgPara]
Code = ETHUSDT
Frequency = 5m

[OrderPara]
order_size = 0.02
```

### .env
```
BINANCE_API_KEY=...
BINANCE_API_SECRET=...
```

## 当前状态

- 引擎稳定运行 ✅
- CSV 日志自动记录 ✅
- 指标正常显示 ✅
- 风控全绿 ✅
- 等待 MA10 上穿 MA30 金叉触发 signal=1

---

*记录人：OpenClaw | 日期：2026-06-29*
